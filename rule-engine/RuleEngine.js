class RuleEngine {
  constructor() {
    // this._options = options
    this.rules = []
    this.gRules = []
  }

  chain(fact, connector) {
    // find matching rules
    this.rules.filter(rule => ru)
  }
}

// This works
module.exports = RuleEngine